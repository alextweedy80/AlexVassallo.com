'use strict';

describe('myApp.RobotView module', function() {

  beforeEach(module('myApp.RobotView'));

  describe('robot view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl_Robot = $controller('ViewCtrl_Robot');
      expect(viewCtrl_Robot).toBeDefined();
    }));

  });
});