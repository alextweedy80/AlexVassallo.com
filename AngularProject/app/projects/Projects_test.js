'use strict';

describe('myApp.ProjectsView module', function() {

  beforeEach(module('myApp.ProjectsView'));

  describe('Projects view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl_Projects = $controller('ViewCtrl_Projects');
      expect(viewCtrl_Projects).toBeDefined();
    }));

  });
});