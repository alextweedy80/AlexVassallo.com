'use strict';

describe('myApp.HeartRateMonitorView module', function() {

  beforeEach(module('myApp.HeartRateMonitorView'));

  describe('HeartRateMonitor view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl_HeartRateMonitor = $controller('ViewCtrl_HeartRateMonitor');
      expect(viewCtrl_HeartRateMonitor).toBeDefined();
    }));

  });
});