'use strict';

describe('myApp.ResumeView module', function () {

    beforeEach(module('myApp.ResumeView'));

    describe('Resume view controller', function () {

    it('should ....', inject(function($controller) {
      //spec body
        var viewCtrl_Resume = $controller('ViewCtrl_Resume');
        expect(viewCtrl_Resume).toBeDefined();
    }));

  });
});